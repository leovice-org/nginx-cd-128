apiVersion: composition.krateo.io/v0-1-0
kind: NginxWebServer
metadata:
  name: nginx-web-server-1
  namespace: default
spec:
  image:
    repository: "nginx"
    tag: "stable"
  service:
    type: "ClusterIP"
